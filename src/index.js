import { CHANNEL } from "./channel";
import createExtensionMiddleware from "./createExtensionMiddleware";
import createExtensionBackgroundProxy from "./createExtensionBackgroundProxy";

export default {
    CHANNEL,
    createExtensionMiddleware,
    createExtensionBackgroundProxy
};
